# tools/ (Phase 3)

Deterministic engines exposed as LLM tools (`get_dose`, `interpret_pcr`,
`list_panel`, `select_pathway`, `answer_from_section`, `ask_clarification`).
Bodies reuse the tested `selection_engine` logic. Empty until Phase 3.
