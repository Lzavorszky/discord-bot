# protocols/ (Phase 2)

One YAML file per protocol, validated against the JSON Schema for the four kinds
(`drug_dose`, `pcr_panel`, `pathway`, `prose`). Empty until Phase 2; the vertical
slice starts with `meropenem.yaml`.
